/** @type {import('next').NextConfig} */
const nextConfig = {

    output: 'standalone',

      // trailingSlash: true,
      // output: 'standalone',
      // images: {
      //     unoptimized: true,
      //   },
      //   forceSwcTransforms: true,
      //   swcMinify: false,
  

};

module.exports = nextConfig;
